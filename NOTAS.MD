<!-- CREA UN TODO LIST -->

# TODO LIST

## Elimina estos registros de la tabla M_TABLAS

- [ ] PRV
- [ ] TDO

### Se omite la columna `Ubigeo` de la tabla `M_CLIENTES`

### SWT_DECL int, si es retencion - M_PROVEE

### tipo de documento ''

### tipo de proveedor: craer

SELECT d_tablas.des_item, d_tablas.num_item FROM d_tablas WHERE cdg_tab = 'PRV' AND swt_item = 1 ORDER BY des_item

### diminutos de codigo de producto es abri_ten

---

eliminar documento

documento tipo de documetnos dtablas SELECT substring(des_item,1,30) as des_item, num_item FROM d_tablas WHERE cdg_tab='TDC' AND num_item <> '003' AND swt_item=1 ORDER BY des_item

guia SELECT substring(des_item,1,30) as des_item, num_item FROM d_tablas WHERE cdg_tab='TPG' AND swt_item = 1 AND num_item = '003' ORDER BY des_item

estadfo swt = 0

---

permisos
usuario nuevo. modulos ocultos o bloqueado

M_OPCION

d_opcion cambiar nombre de cliente a venta
d_usuari son los permisos

d_opcion es para listar el select de asignacion de opciones: modulo - opcion - estado

cdg_opc es llave CDG_OPC modulo general

m_opcion para saber CDG_OPC
